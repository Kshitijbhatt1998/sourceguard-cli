# .sourceguardignore — copy to .sourceguardignore in your project root
# Syntax: one pattern per line, gitignore-style. Lines starting with # are comments.

# Test fixtures with fake credentials
tests/fixtures/*
tests/data/*

# Vendor / third-party code
vendor/*
third_party/*

# Generated files
*.generated.*
dist/*
build/*
